server {
    listen 80;
    root /usr/share/nginx/html;

    # Client-side routes (/jobs/42, /login?token=...) all load the app, which then routes itself.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
